# PST 构型优化资料整理：CosMatter 试用审计与改进计划

> 审计日期：2026-09-06  
> 审计对象：`pst_md_algo_20260906`、`pst_physics_algo_20260906` 两个本地受控运行。  
> 信任边界：本文只统计脱敏运行工件和候选元数据；6 个 Sciverse 短上下文来自委托自动试点，不是人工 Source Map，也不是已接受科学证据。本文不复制全文、私有路径、令牌、请求载荷或候选短摘录。

## 1. 本次怎样操纵 CosMatter

两个问题分别进入“无代理算法效率”和“物理机制引导”路线。每条路线都按同一控制链执行：

```text
MissionBrief
  → 舰队分派（route_diagnostics）
  → DeepSeek 不可信计划草案
  → 人工收紧并批准 FlightPlan
  → Sciverse 主检索 + 反例检索
  → 来源回执审计
  → 委托自动试点筛选（每次最多 3 篇）
  → Sciverse 受控上下文读取
  → 工作流/运行不变量/敏感工件审计
```

两次运行都停在正式 `screening`：自动试点可以验证检索和全文访问链，但不能冒充人工筛选，因而不会自动进入正式 Source Map、事实抽取、Gap 或报告阶段。

## 2. 可复核统计

| 指标 | 无代理算法路线 | 物理机制路线 | 合计/说明 |
|---|---:|---:|---|
| 已批准主检索 + 反例检索 | 5 + 4 | 8 + 4 | 21 次 |
| 去重后的候选记录 | 160 | 207 | 367 个唯一 `document_id` |
| 候选来源记录 | 160 | 215 | 375 条来源关联 |
| 供应商回执 | 12 | 15 | 21 个检索回执 + 6 个上下文回执 |
| 来源—回执覆盖率 | 100% | 100% | 两次审计均通过 |
| DOI 非空候选 | 0 | 0 | 367 条候选均缺 DOI |
| `is_content_accessible=true` | 160 | 207 | 全部标真；只实际抽查了 6 条 |
| 委托试点纳入/待元数据审核 | 3 / 157 | 3 / 204 | 共纳入 6、待审 361 |
| 成功读取受控上下文 | 3 | 3 | 6/6 成功，每段不超过 4000 字符 |
| 阅读路线条目 | 12 | 12 | 两条路线均只标成 `primary` |
| 已筛选候选进入阅读路线 | 1/3 | 0/3 | 筛选结果没有驱动路线重排 |
| 标题精确重复组 | 2 组/4 条 | 1 组/2 条 | 合计 367 条仅有 364 个规范化唯一标题 |
| 运行不变量审计 | 通过 | 通过 | 没有关系破坏 |
| 敏感工件发现类别 | 0 | 0 | 两次审计均为 clean |

额外的标题级诊断只用于发现产品问题，不是人工相关性评测：

- 无代理算法路线用预先写明的算法关键词机械匹配到 16/160；自动阅读路线匹配到 5/12。
- 物理机制路线用宽泛的铁电/钙钛矿关键词匹配到 136/207、阅读路线匹配到 9/12，但人工浏览标题后，真正明确涉及 `(Pb_xSr_{1-x})TiO3` 的候选只有 3 条。
- 这说明宽泛语义相关分数可以找到机制类比，却不能单独保证材料、目标性质、方法约束和“禁止代理模型”同时满足。

本次还观察到 5 次本地拒绝：系统 Python 直接执行模块失败 2 次；3 个上下文请求因 `limit=8192` 超出允许的 `200–4000` 而被拒绝。全部安全失败关闭，未生成部分全文文件；改用项目 `.venv` 和 `limit=4000` 后成功。拒绝事件未进入运行 `events.jsonl`，因此当前只能从交互记录而非运行审计中统计。

## 3. 整理后的科学方案

### 3.1 主算法：MD Response-guided Motif Tempering（MRMT）

相比在 512 位占位串上盲目执行遗传或退火，主算法应在三个尺度上搜索：

1. **化学构型尺度**：固定 `n_Pb/n_Sr`，记录前几壳层 Warren–Cowley 参数、Pb/Sr 团簇尺度、界面密度与方向；
2. **成分波尺度**：以超胞允许的离散 `S(k)` 模式生成层状、柱状、棋盘状、长波团簇和 SQS/随机参照；`8×8×8` 只允许与周期 8 相容的波长；
3. **响应场尺度**：从已经执行的 MD 轨迹提取局域极化、TiO6 倾斜、局域应变、极化协方差、极化—应变交叉相关和软模空间分布。

局域响应只用于提高下一次固定组分交换、界面移动或集体成分波操作的提议概率。新构型是否接受、如何 Pareto 排序和最终介电/压电值，仍必须由新一轮 MD 实测；不得建立 `configuration → property` 预测器。

### 3.2 物理判断边界

- Pb²⁺ 与 Sr²⁺ 是等价价离子，不应把“电荷平衡”作为 A 位排列的主要先验；重点是 Pb 的极化软度/孤对电子效应、尺寸失配、局域应变和八面体转动耦合。
- 最大局域或平均极化不等于最大介电响应。受控上下文提示，PbTiO3 畴壁的较小局域极化、较浅势能面可以对应更高局域易极化性。
- 单一近邻计数或单一短程有序参数不足。类比固溶体研究明确把方向排列、空间相关和局域应变列为简单邻居模型遗漏项。
- 化学有序畴尺寸也不能单独解释介电响应；相似畴尺寸可以对应不同响应，因此候选生成必须同时覆盖有序方式、界面和局域化学环境。
- 上述信息来自 PST 邻近体系和机制类比，目前只能指导算法设计，不能当作 PST 已验证规律。

### 3.3 执行结构与对照

```text
随机 / SQS / 成分波 / 团簇 / 层状的物理分层初始库
                         ↓
短 MD：全局目标 + 局域响应场
                         ↓
响应引导的交换 / 界面移动 / 集体模式操作
                         ↓
多副本回火 + 异步逐级晋级
                         ↓
OCBA/MOCBA 分配独立复算预算
                         ↓
统一长 MD + 独立种子 + Pareto 档案
```

必须在相同 MD 总步数与并行资源下比较四组：均匀随机、普通固定基数 EDA、只用短程有序引导、完整 MRMT。这样才能区分改进来自物理提议、预算分配还是偶然涨落。

## 4. 流程问题与根因

| 优先级 | 问题 | 本次观测 | 根因判断 |
|---|---|---|---|
| P0 | 新终端入口不稳定 | 系统 Python 找不到 `cosmatter`，仓库 `.venv` 正常 | 缺少统一启动器和启动前环境自检 |
| P0 | 阅读路线没有消费筛选结果 | 两次仅 1/3、0/3 已纳入候选进入路线 | 路线按全局候选分数生成，没有 selected-first 重排 |
| P0 | 反例检索没有进入阅读路线 | 两条 12 项路线均为 `primary` | 阅读路线没有强制主证据/反例配额 |
| P0 | DOI 完整性为零 | 367/367 候选 DOI 为空 | `agentic-search` 片段结果未经过 meta-search/Crossref/OpenAlex 元数据补全 |
| P1 | 材料与方法约束被语义相似度稀释 | 首个算法查询混入流体、制造、毒素等文献；物理路线直接 PST 候选很少 | 缺少材料别名、性质、方法和排除项的分面重排 |
| P1 | 内容可访问状态过于乐观 | 367/367 均为 true，只验证 6 条 | 将上游可访问提示当成确定状态，缺少确认时间和失败态 |
| P1 | 自动试点在状态图中看起来仍“未开始” | 6 条上下文已读取，正式状态仍等待人工/parse blocked | 正式证据轨道与委托试点轨道没有并列投影 |
| P1 | Sciverse 上下文缺少直接 Source Map 路由 | 已有受控上下文文件，但正式解析阶段不能消费 | 当前 Source Map 流程主要围绕 MinerU 完成任务设计 |
| P2 | 参数边界发现太晚 | `--help` 未显示 200–4000，连续 3 次请求才收到运行时拒绝 | CLI schema 没把范围投影到帮助和前端表单 |
| P2 | 安全拒绝不进入运行统计 | 5 次本地拒绝没有对应安全事件 | 失败前不写审计虽可避免污染，但失去可观测性 |
| P2 | 精确标题重复仍存在 | 3 组重复标题对应不同 `document_id` | 缺 DOI 时只按文献 ID 去重，不做可审核的标题候选对账 |

### 4.1 第一批修复复验（2026-09-07）

- 根目录新增 `cosmatter.ps1`，在新 PowerShell 中固定调用仓库 `.venv`；`check-config` 已通过，且只输出五个配置存在性布尔值。
- 阅读路线现在读取与当前候选指纹一致的人工筛选；没有人工筛选时，可读取明确分离的委托自动试点筛选。过期筛选会安全拒绝，筛选结果只影响阅读次序，不提升为科学证据。
- 两个真实试点重建后，算法路线和物理路线均达到已筛选候选 `3/3` 入路；算法路线为 `primary=11, counterevidence=1`，物理路线为 `primary=10, counterevidence=2`。
- `sciverse-read-context --help` 已显示 `limit=200–4000` 与非负 offset。CLI/MCP/UI 共用参数 schema、DOI 补全和全文三态仍未完成，不能据此关闭全部 P0/P1。

## 5. 后续改进计划

### P0：入口、路线与元数据

1. 新增根目录 `cosmatter.ps1` 或等价启动器，固定调用 `.venv`；启动时只报告 Python、包版本和配置存在性，不回显密钥。
2. `build-reading-guide` 改为 selected-first：已纳入候选必须优先出现；其后按任务分面相关性补足，并保证至少一条主检索和一条反例检索路线。
3. 在 `agentic-search` 后增加只读元数据补全：用 Sciverse meta-search、Crossref/OpenAlex 规范化 DOI、标题、年份；冲突保留候选映射，禁止模糊自动合并。
4. 阅读路线显示“为何入选”：材料命中、性质命中、方法命中、反例角色、全文状态；语义分数不能成为唯一理由。

验收：新终端单命令可运行；3/3 已筛选候选进入下一版路线；路线同时含 primary/counter；已知 DOI 的冻结夹具补全率 100%，冲突 DOI 安全失败。

进度：前两项及其两个真实运行复验已完成；第 3 项 DOI 补全和第 4 项结构化“为何入选”仍待实现。

### P1：检索质量、访问状态和试点轨道

1. 将查询拆成 `exact_material`、`mechanism_analogue`、`algorithm` 三轨，分别设配额；PST 精确轨必须匹配化学式/别名，禁止被宽泛钙钛矿结果占满。
2. 增加任务分面重排与显式排除条件，例如 `surrogate/model prediction` 只能进入反例轨，不得混入无代理主路线。
3. 用独立人工标题集报告每轨 Precision@K、nDCG@K 和直接材料覆盖，不能用本文的机械关键词计数冒充指标。
4. 将内容状态改为 `provider_advertised → confirmed → failed/expired`；只有成功读取后才能显示 confirmed，并保存确认时间与安全失败码。
5. UI 同时显示“正式证据轨”和“自动试点轨”；后者可显示已经完成的筛选/读取计数，但不能解锁 EvidenceCard。

验收：冻结人工集上，新分面重排相对当前排序的 nDCG@12 提升至少 0.15；未实际读取的候选不再显示 confirmed；试点状态不再被误写成正式 parse completed。

### P1：Sciverse 上下文到受控 Source Map

新增 `prepare-sciverse-context-review`：只接收已确认的上下文回执，将私有内容切成有界候选段，并生成与 `document_id + offset + content_hash` 绑定的审阅池。自动试点只能建立 `delegated_automated_trial_source_map`；人工版本仍需显式选择和确认。

验收：错配文献、offset、哈希、过期确认或运行 ID 时全部拒绝；运行目录不出现上下文正文和私有路径；委托 Source Map 不能进入正式事实或报告。

### P2：参数契约与拒绝遥测

1. 从同一参数 schema 生成 CLI 帮助、MCP schema 和前端约束，使 `limit: 200–4000` 在调用前可见。
2. 为本地验证拒绝增加无内容安全计数：只记录命令类别、原因码、时间和 run ID，不记录参数值、路径、查询或正文。
3. 增加“候选重复待对账”队列：只有 DOI 一致或人工确认后才合并；标题相同只能提示，不能自动认定同文献。

验收：越界参数在 provider 调用前被一致拒绝；CLI/MCP/UI 三处范围测试同源；运行统计可重建失败次数，同时敏感工件审计仍为零发现。

### P2：MRMT 计划型实现

先实现固定组分位串、对称性规范化、短程有序/成分波描述符、物理提议操作和合成目标消融；随后接只读 MD 回执与预算账本。真实 MD 仍需独立执行授权。

验收沿用 [`PST_MD_ONLY_CONFIGURATION_SEARCH_PLAN.zh-CN.md`](PST_MD_ONLY_CONFIGURATION_SEARCH_PLAN.zh-CN.md) 的 L0–L4，并额外要求所有物理描述符只改变提议概率，`property_prediction_count = 0`。

## 6. 完成定义

只有当 P0/P1 的回归夹具通过、两条试点可在新终端复跑、阅读路线正确消费筛选与反例、Sciverse 上下文能进入受控审阅池，并且 MRMT 在同预算合成消融中可复现地优于随机提议时，才可称为“资料整理与算法规划链已完善”。真实介电/压电优化仍是后续独立试点，不由本文完成。
