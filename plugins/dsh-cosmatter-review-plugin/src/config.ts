export interface CosMatterReviewConfig { baseUrl?: string; timeoutMs?: number; maxResponseBytes?: number }
export interface NormalizedConfig { baseUrl: string; timeoutMs: number; maxResponseBytes: number }
export function normalizeConfig(config: CosMatterReviewConfig = {}): NormalizedConfig { const parsed = new URL(config.baseUrl ?? 'http://127.0.0.1:8765'); if (parsed.protocol !== 'http:' || parsed.hostname !== '127.0.0.1' || parsed.username || parsed.password || parsed.pathname !== '/' || parsed.search || parsed.hash) throw new Error('CosMatter review plugin only permits a bare http://127.0.0.1 endpoint'); const timeoutMs = config.timeoutMs ?? 10_000; const maxResponseBytes = config.maxResponseBytes ?? 262_144; if (!Number.isInteger(timeoutMs) || timeoutMs < 100 || timeoutMs > 30_000 || !Number.isInteger(maxResponseBytes) || maxResponseBytes < 4_096 || maxResponseBytes > 524_288) throw new Error('CosMatter review plugin config is invalid'); return { baseUrl: parsed.toString().replace(/\/$/, ''), timeoutMs, maxResponseBytes } }
