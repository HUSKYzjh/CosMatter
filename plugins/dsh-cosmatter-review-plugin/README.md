# CosMatter review plugin for DeepSeek Harness

This bundle is the human gate between metadata retrieval and full-text work. It exposes a bounded candidate metadata checklist and records a complete human decision set. It does not call a provider, retrieve full text, dispatch MinerU, create Source Maps, or accept EvidenceCards.
