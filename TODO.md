# CosMatter 待办

> 完成状态以可复跑的测试、审计工件或版本发布为准，而不是界面截图。

## 下一阶段：舰队运行时

- [x] 实现星际调度中心：按任务类型选择唯一主舰队，并记录分派理由。
- [x] 实现通用航行工位与 `FleetHandoff` 门禁。
- [x] 实现证据巡检舰队：原文定位镜、证据比对台、条件记录仪（`facilities.py` 的定位、条件完整性与发布门禁；来源映射仍须人工复核）。
- [x] 实现航道诊断舰队：航迹叠加仪、条件差分舱、反证探测器（条件矩阵、反例边界与 Gap 门禁均保留在本地审计链内）。
- [x] 为每台设施增加输入/输出 Schema、工具白名单、失败路径与单测（封闭静态契约目录覆盖全部 15 项设施；配置加载、只读 API 投影与前端渲染均有回归测试）。

## UI / HTML 界面

- [x] 新建 `web/` 静态前端骨架：`index.html`、`styles.css`、`app.js`；第一版不携带密钥、不直接调用第三方 API。
- [x] 制作“任务舰桥”输入页：研究问题、材料体系、性质、范围和数据源策略。
- [x] 制作“舰队航行面板”：显示主舰队、当前航行工位/专用设施、运行状态、预算和退回原因。
- [x] 制作“证据卡查看器”：显示主张、条件字段、审核状态和 `document_id + locator + quote`；仅显示获准公开的短摘录。
- [x] 制作“条件星图/分歧矩阵”视图：按条件分组展示支持、反驳和未知，不把图谱布局当作因果结论。
- [x] 定义 Python 到 UI 的只读 JSON 合约：`MissionBrief`、设施状态、`EvidenceCard`、`VerificationDecision`、`MissionReport`。
- [x] 增加本地 HTTP API 后端或导出 JSON 文件模式；浏览器端不得读取 `.env`、运行日志原文或受限论文。
- [x] 添加最小 UI 冒烟测试：静态资源可加载、无密钥泄露、证据卡链接字段可渲染。
- [x] 增加“运行工件选择器”：从用户明确选择的 `ui.json` 导入，显示工件版本、生成时间、文件大小与可显示数据量；不自动扫描 `runs/` 或读取 `.env`。
- [x] 增加证据卡筛选与追溯交互：在同一任务中按立场、条件字段和关键词过滤；只投影已批准卡，并保留 `document_id + locator` 定位信息。
- [x] 增加条件星图/分歧矩阵的展开视图：展示同一条件簇中的支持证据、反证、差异字段与未知项，并明确区分“证据事实”“系统推断”和“待核查项”。
- [x] 建立多页面舰桥：任务舰桥、研究工作流、星图网络和研究拓展各自承担导入/审查、门禁投影、关系浏览和不可信规划草案导出；全页支持深色、浅色和护眼主题。
- [x] 增加 FrontierLens 式 Guided Cards：从批准计划、候选历史和已接受证据生成有界阅读路线，并显式保留主检索/反例检索与内容访问状态。
- [x] 增加语义关系星图：用候选论文、证据、条件簇和未知项组成图谱，图例明确区分检索候选、出处、审核支持/反驳与待核查关系。
- [x] 增加受限论文导读：从阅读路线查看候选、访问状态、`document_id + locator` 与已批准短证据；没有授权解析工件时不显示全文。
- [x] 增加任务运行时间线：将已批准计划、主检索、反例检索、证据审核、条件诊断和报告交付映射为只读白名单动作；不输出失败原文、actor、请求 ID 或 payload。
- [x] 增加本地演示与可访问性验收：为键盘操作、窄屏布局、空工件、错误 JSON、含条件矩阵的导出工件、跨源对账修订摘要、无指标的评测准备概览、任务定义期的本地问题集审核、冻结语料相关性审核，以及合成演示不解锁真实研究路径补充浏览器级测试与截图基线（`npm run test:e2e`：15/15 通过）。
  - [x] Playwright / 本机 Edge：390 px 窄屏工作区无横向溢出、截图基线，以及 BFO 模板的键盘激活与 `aria-pressed` 回归。
  - [x] 补充空工件、错误 JSON 和含条件矩阵的真实导出工件的浏览器场景；使用 `examples/ui-demo/route_diagnostics.json` 脱敏夹具，不读取 `.env`、运行目录或私有全文。
- [x] 评估并提供仅监听 `127.0.0.1` 的本地只读服务：`preview-ui --solid --run-id <run_id>` / `start-solid-preview.ps1 -RunId <run_id>` 仅暴露指定的已导出 `/ui.json`；未启用 `--api` / `-Api` 时拒绝所有 API 写入，也不代理模型、论文全文或第三方检索 API。

## 评测与发布

- [ ] 建立冻结问题集和人工核查记录。
  - [x] 已提供 8 条分层 BFO 问题的空白审阅模板、五项问题质量检查、不可覆盖的冻结命令与聚合审计工件；空白或部分审阅不能冻结。
  - [x] 已将经过双工件哈希/数量/任务绑定校验的冻结问题集聚合状态接入只读 UI；仅展示纳入数量和四级证据分布，不输出题目、评语、标识或哈希，也不冒充评测指标。
  - [x] 真实评测运行记录 schema 1.1 已绑定冻结问题集 ID、题数和内容哈希；提交准备、执行索引和最终 ZIP 均验证成对工件，完成态包会携带冻结问题与无评语聚合审计。
  - [x] 任务定义页已提供本地问题集审核台：逐题决定、五项检查、理由、独立声明和导出均有门禁；草稿仅暂存同源浏览器会话，声明不持久化，错误导入不会覆盖现有草稿。
  - [ ] 仍须由独立研究者逐题完成纳入决定、质量检查和理由，形成首份真实 `frozen_question_set.json`；模板或测试审阅不算人工核查。
- [ ] 报告 Citation Precision、Condition Completeness、Contradiction Precision 与复跑一致性。
- [x] 增加数据治理、评测和安全文档：[`docs/data-governance.md`](docs/data-governance.md)、[`docs/evaluation.md`](docs/evaluation.md) 与 [`SECURITY.md`](SECURITY.md) 已纳入 README，并由无私有路径/凭据的回归测试覆盖。
- [x] 已选择 MIT 许可证，并确认仓库为公开状态；后续发布仍须逐次核对私有运行工件、受限 PDF 与密钥未进入版本库。
